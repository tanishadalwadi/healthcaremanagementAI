/**
 * layout/ — nav shells, sidebar
 *
 * Components to be built in Phase 4:
 *   NurseSidebar  (fixed 212px, 5 nav items, user chip at bottom)
 *   NurseShell    (sidebar + main content area)
 *   DoctorShell   (single centered panel, max-w ~760px)
 */
