/**
 * hooks/ — shared React hooks
 *
 * To be added per phase:
 *   usePatients()         — wraps lib/api.ts getPatients()
 *   usePatient(id)        — wraps lib/api.ts getPatientById()
 *   useWorkflow(id)       — wraps lib/api.ts getWorkflow()
 *   useExpandState()      — per-workflow-group open/closed toggle state
 */
